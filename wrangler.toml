# 基础必配
name = "你的项目名"
compatibility_date = "2026-02-01" # 写最新日期即可
main = "src/index.js" # 关键：指定入口文件为根目录的index.js

# 新版静态资产配置（替换旧[site]）
[assets]
binding = "MY_ASSETS" # 和index.js中调用的一致
directory = "./dist" # 静态资源目录
